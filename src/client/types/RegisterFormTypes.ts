export type Inputs = {
    example: string;
    exampleRequired: string;
  };

export type FormValues = {
    username: string;
    email: string;
    dob: Date;
    password: string;
    createdOn: Date;
}